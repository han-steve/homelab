apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NOTE: simplefin-secret.yaml has a placeholder token.
# Apply the real secret before ArgoCD sync:
#   kubectl -n monitoring create secret generic simplefin-config \
#     --from-literal=setup-token='<token-from-app.simplefin.org>'

resources:
  - simplefin-secret.yaml
  - simplefin-cronjob.yaml
