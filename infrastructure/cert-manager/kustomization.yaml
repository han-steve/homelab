apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# cert-manager CRDs and helm chart are installed via bootstrap.sh section12_cert_manager
# These resources are applied after cert-manager is running:
resources:
  - clusterissuers.yaml
  - service-certificates.yaml
