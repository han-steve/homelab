# Homelab Ingress Resources
# All services exposed via nginx-ingress at 192.168.1.21
# TLS terminated at nginx using wildcard cert *.homelab
#
# DNS: All *.homelab / *.homelab.local → 192.168.1.21 (nginx-ingress)
# Exception: direct IP access still works for all services
#
# Service map:
#   jellyfin.homelab      → jellyfin/jellyfin:8096         (HTTP backend)
#   grafana.homelab       → monitoring/prometheus-grafana:80 (HTTP backend)
#   argocd.homelab        → argocd/argocd-server:80         (HTTP backend, --insecure)
#   apitable.homelab      → apitable/apitable-gateway:80    (HTTP backend)
#   budget.homelab        → actual-budget/actual-budget:5006 (HTTPS backend)
#   ocis.homelab          → ocis/ocis:9200                  (HTTPS backend)

---
# Jellyfin — Media Server
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jellyfin
  namespace: jellyfin
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "0"      # unlimited for media uploads
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - jellyfin.homelab
        - jellyfin.homelab.local
      secretName: jellyfin-tls
  rules:
    - host: jellyfin.homelab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jellyfin
                port:
                  number: 8096
    - host: jellyfin.homelab.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jellyfin
                port:
                  number: 8096

---
# Grafana — Monitoring Dashboard
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: monitoring
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - grafana.homelab
        - grafana.homelab.local
      secretName: grafana-tls
  rules:
    - host: grafana.homelab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prometheus-grafana
                port:
                  number: 80
    - host: grafana.homelab.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prometheus-grafana
                port:
                  number: 80

---
# ArgoCD — GitOps Controller
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd
  namespace: argocd
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - argocd.homelab
        - argocd.homelab.local
      secretName: argocd-tls
  rules:
    - host: argocd.homelab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 80
    - host: argocd.homelab.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 80

---
# APITable — Spreadsheet/Database
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: apitable
  namespace: apitable
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - apitable.homelab
        - apitable.homelab.local
      secretName: apitable-tls
  rules:
    - host: apitable.homelab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: apitable-gateway
                port:
                  number: 80
    - host: apitable.homelab.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: apitable-gateway
                port:
                  number: 80

---
# Actual Budget — Personal Finance
# Backend serves HTTPS on 5006; nginx proxies with ssl verify disabled
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: actual-budget
  namespace: actual-budget
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/proxy-ssl-verify: "off"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - budget.homelab
        - budget.homelab.local
        - actual-budget.homelab
        - actual-budget.homelab.local
      secretName: actual-budget-tls
  rules:
    - host: budget.homelab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: actual-budget
                port:
                  number: 5006
    - host: budget.homelab.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: actual-budget
                port:
                  number: 5006
    - host: actual-budget.homelab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: actual-budget
                port:
                  number: 5006

---
# oCIS — Cloud Storage (Nextcloud-like)
# Backend serves HTTPS on 9200; nginx proxies with ssl verify disabled
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ocis
  namespace: ocis
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/proxy-ssl-verify: "off"
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ocis.homelab
        - ocis.homelab.local
      secretName: ocis-tls
  rules:
    - host: ocis.homelab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ocis
                port:
                  number: 9200
    - host: ocis.homelab.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ocis
                port:
                  number: 9200
