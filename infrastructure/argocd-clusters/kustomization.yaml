apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ArgoCD cluster secrets are generated at runtime via scripts/register-vclusters.py
# They contain client certificates and are NOT committed to git.
# See infrastructure/argocd-clusters/vcluster-prod.yaml for instructions.
resources: []
