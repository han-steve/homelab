# Talos — generated secrets and machine configs (contain cluster tokens)
# Store in 1Password. Generate with: talosctl gen secrets + talosctl gen config
talos/secrets.yaml
talos/controlplane.yaml
talos/worker.yaml
talos/worker-gpu.yaml
talos/talosconfig
talos/.m2-schematic-id
talos/.gpu-schematic-id

# OS
.DS_Store

# Credentials
.env
.env.*
rclone.conf

# Dashboard
dashboard/node_modules/
dashboard/.next/
dashboard/out/
docs-site/node_modules/
docs-site/build/
docs-site/.docusaurus/
